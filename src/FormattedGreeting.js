import React from 'react';
import {FormattedMessage} from 'react-intl';

function FormattedGreeting() {
  return (
    <div>
      <FormattedMessage id="greeting.correct" />
    </div>
  );
}

export default FormattedGreeting;
